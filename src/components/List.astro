---
let { block, is_first, is_last } = Astro.props;

const listType = block.type === 'bulleted_list_item' ? 'ul' : 'ol'

---

{
  is_first ?
  block[block.type].is_first && <Fragment set:html={`<${listType}>`} /> :
  block[block.type].is_last && <Fragment set:html={`</${listType}>`} />
}
